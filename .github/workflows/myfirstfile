name: Github actions workflow
on: push
jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
        - name: Learning Github actions
          run: echo "I should get new project allocation"
        - name: Manifesting
          run: Manifesting to get project allocation asap